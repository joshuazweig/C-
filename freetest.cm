int main()
{
	stone x;
	stone y;
	stone z;

	x = "10";

	//y = x + x + x + x + x + x + x + x + x + x + x + x + x + x + x + x;
	//y = x + x;
	y = x + x + x;
	print_stone(y);

	//z = x + x;
	//print_stone(z);
	z = y + y + x;
	print_stone(z);	
}
