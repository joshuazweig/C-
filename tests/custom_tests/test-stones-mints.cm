int main()
{
  stone x;
  stone y;
  stone z;
  stone p;

  mint m;
  mint n;
  mint o;

  x = "15352395";
  y = "-1123053158"; 
  z = "-1";
  p = "65537"; 

  // some arithmetic with negative exponents seems off?
  // also, if you "mod" a negative bignum, the result is still negative. don't 
  // think we want this behavior

  m = <x, p>;
  n = <z, p>;
  
  print_mint(m);
  print_mint(n);

  o = m + n;
  printf("print_mint\n");
  print_mint(o);
  
  o = m - n;
  printf("print_mint\n");
  print_mint(o);
  
  o = m * n;
  printf("print_mint\n");
  print_mint(o);
  
  o = m ^ n;
  printf("print_mint\n");
  print_mint(o);

  o = m ^ z;
  printf("print_mint\n");
  print_mint(o);
  return 0;
}
